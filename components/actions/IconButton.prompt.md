Square, icon-only button for toolbars and dense controls. Requires an aria-label.

```jsx
<IconButton aria-label="Settings" variant="ghost"><Icon name="settings" /></IconButton>
```

Variants: primary · secondary · ghost. Sizes match Button (26/32/40px).
