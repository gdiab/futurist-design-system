Compact KPI readout for dashboards.

```jsx
<Stat label="P99 latency" value="142" unit="ms" delta="12% wk" trend="down" />
```

Trend colors delta green (up) / red (down). Note: for latency "down" is good — set trend accordingly.
